[package]
name = "personhood"
type = "bin"
authors = ["Trulib Ltd"]
compiler_version = ">=0.34.0"

# STAND-IN personhood circuit used to make the AZNS ZKPassport gate testable
# without the real ZKPassport mobile app. Its UltraHonk proof is verified by
# AZNS.register_first via verify_honk_proof. Swap this circuit (and re-pin the
# contract's vkHash) for the real ZKPassport circuit in production.

[dependencies]
